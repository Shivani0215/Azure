trigger:
- master

pr:
- '*'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UseAWSConnection@1
  inputs:
    awsConnection: 'YourAWSServiceConnection'  # Replace with the name of your AWS service connection
    scriptType: 'inline'
    inlineScript: 'aws s3 ls'
